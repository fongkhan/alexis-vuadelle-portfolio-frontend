---
interface Props {
  title: string;
  slug: string;
  techStack: { technology: string }[];
  featuredImage?: { url: string; alt?: string };
}

const { title, slug, techStack, featuredImage } = Astro.props;
---

<a href={`/projects/${slug}`} class="block group">
  <article class="glass rounded-2xl overflow-hidden card-hover h-full flex flex-col items-start bg-gradient-to-b from-surface/80 to-surface/40">
    <div class="relative w-full aspect-video overflow-hidden bg-black/20 border-b border-white/5">
      {featuredImage?.url ? (
        <img 
          src={`http://localhost:3000${featuredImage.url}`} 
          alt={featuredImage.alt || title}
          class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
        />
      ) : (
        <div class="w-full h-full flex items-center justify-center bg-gradient-to-br from-primary/20 to-secondary/20">
          <span class="text-white/30 text-sm font-medium">No Image</span>
        </div>
      )}
      <div class="absolute inset-0 bg-gradient-to-t from-surface via-transparent to-transparent opacity-80"></div>
    </div>
    
    <div class="p-6 flex flex-col gap-4 w-full flex-grow">
      <h3 class="text-xl font-bold text-white group-hover:text-primary transition-colors">{title}</h3>
      
      {techStack && techStack.length > 0 && (
        <div class="flex flex-wrap gap-2 mt-auto">
          {techStack.map((tech) => (
            <span class="px-3 py-1 rounded-full text-xs font-medium bg-white/5 text-text-muted border border-white/5">
              {tech.technology}
            </span>
          ))}
        </div>
      )}
    </div>
  </article>
</a>
